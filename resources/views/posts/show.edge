@component('layouts/main', { title: 'Show Article' })
  @slot('main')
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>{{ article.title }}</h1>
    <div>
      <form method="POST" action="{{ route('articles.edit', [article.id]) }}>
        {{ csrfField() }}
        <button type="submit" class="btn btn-sm btn-warning">Edit</button>
      </form>
      <a href="{{ route('articles.index') }}" class="btn btn-secondary ms-2">Back to All Articles</a>
    </div>
  </div>

  <div class="card mb-4">
    <div class="card-body">
      <div class="mb-4">
        <h5>Title</h5>
        <div class="content border rounded p-3 bg-light">
          {{ article.title }}
        </div>
        <h5>Body</h5>
        <div class="content border rounded p-3 bg-light">
          {{ article.body }}
        </div>
      </div>
      <div class="row">
        <div class="col-md-6">
          <p><strong>Created:</strong> {{ article.createdAt.toFormat('dd/MM/yyyy HH:mm') }}</p>
          <p><strong>Last Updated:</strong> {{ article.updatedAt.toFormat('dd/MM/yyyy HH:mm') }}</p>
        </div>
      </div>
    </div>
  </div>
  @end
@end